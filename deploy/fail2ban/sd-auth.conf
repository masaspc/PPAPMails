# Secure Download - fail2ban設定
# /etc/fail2ban/jail.d/sd-auth.conf

[sd-auth]
enabled = true
port = http,https
filter = sd-auth
logpath = /var/log/secure-download/access.log
maxretry = 10
findtime = 600
bantime = 3600
action = iptables-multiport[name=sd-auth, port="http,https", protocol=tcp]

# フィルター定義
# /etc/fail2ban/filter.d/sd-auth.conf
#
# [Definition]
# failregex = ^.*"POST /d/.*/verify HTTP/.*" (401|403) .* <HOST>
#             ^.*"POST /d/.*/request-code HTTP/.*" (403|429) .* <HOST>
# ignoreregex =
